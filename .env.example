# PostgreSQL 本地开发配置
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=travel_agent
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password

# 聊天模型使用 OpenAI 兼容接口，例如 qwen-plus
TOURISM_AGENT_MODEL=your_chat_model
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=your_openai_compatible_base_url

# Conversation RAG 重排序；填写所使用供应商提供的 qwen3.7-text-rerank 调用地址
TOURISM_AGENT_RERANK_URL=your_rerank_api_url
# 当前先保留全部 Rerank 结果，完成实测后再提高该阈值
RAG_RERANK_SCORE_THRESHOLD=0.0
# Chunk 两两余弦相似度达到该值时，只保留 Rerank 分数更高的一条
RAG_DEDUP_SIMILARITY_THRESHOLD=0.98
# pgvector 最小候选池；实际取 max(该值, 最终请求数量)
RAG_CANDIDATE_LIMIT=20

# 默认关闭，开启后真实模型测试会产生网络请求和模型费用
RUN_LLM_INTEGRATION=false

# 旅行查询服务配置；QWeather Host 使用控制台分配的 API Host，不包含路径
QWEATHER_API_HOST=your-api-host.qweatherapi.com
QWEATHER_API_KEY=your_qweather_api_key
AMAP_WEB_SERVICE_KEY=your_amap_web_service_key
TAVILY_API_KEY=your_tavily_api_key

# 默认关闭，仅在本地验证 npm Tavily MCP Server 启动链路时设为 1
RUN_MCP_INTEGRATION=0

# 项目日志同时输出到控制台和轮转文件
TOURISM_LOG_LEVEL=INFO
TOURISM_LOG_FILE=logs/tourism-agent.log
