# Tourism Agent 项目开发约定

## 1. 项目目标

本项目是一个基于 LangGraph 的旅行 Agent。项目应优先完成可运行、可验证的旅行核心流程，保持结构清晰，避免为了可能出现的未来需求提前建设复杂平台。

## 2. MVP 总体范围

MVP 可以逐步覆盖以下能力：

1. 理解用户旅行意图，同时保留原始表达并提取可结构化的旅行条件。
2. 生成、校验和修改旅行计划。
3. 搜索并比较交通、酒店、景点等旅行产品或信息。
4. 在用户明确确认后执行预订；支付通过外部支付服务处理，不自行保存银行卡信息。
5. 查询订单，并支持基础的取消、改签和退款流程。

以上是 MVP 的总体边界，不代表必须在一个阶段全部实现。每次只实现用户当前明确要求的阶段。

## 3. 当前第一步

当前阶段只实现以下垂直链路：

```text
用户输入
→ API 运行状态检查
→ 根图理解 Agent
→ 确定性路由函数
→ Planning Agent 持续 ReAct
→ 按需提问和调用 Tools
→ 如产生或修改行程，用户确认后写入 CurrentItinerary
→ 后端分别返回对话结果和当前行程
```

当前阶段不实现真实预订、支付、订单、退改签、数据库和生产部署。后续只有在用户明确进入相应阶段后才增加这些能力。

## 4. 架构边界

- 当前架构基线见 `docs/architecture.md`；专题说明不得与其冲突。
- 起步阶段采用模块化单体，不拆分微服务。
- 根图中的理解 Agent 只负责输出受约束的路由意图，实际跳转由确定性路由函数完成。
- 每个业务模块只设置一个 Agent，模块内部优先通过 Tools 提供细分能力。
- ReAct 只用于旅行研究、信息获取和计划生成等开放任务。
- Tools 默认只读，并通过明确的输入输出模型暴露能力。
- 金额、时间、库存、报价有效期和订单状态等关键判断使用确定性代码。
- 预订、支付、取消和退款等副作用流程必须由确定性工作流控制，并要求用户明确批准。
- Graph State 只保存可恢复的工作流数据；客户端、密钥、连接和运行时依赖不得放入 State。
- Conversation 保存 raw 历史并只追加，已经发送的消息不得编辑、替换或自动合并；TripContext 使用无固定字段的动态结构；CurrentItinerary 只保留当前文本版本。
- 运行期间禁止用户主动插话；用户必须先取消，再提交一条新消息并从根图重新执行。
- 取消只停止后续执行并清理临时 checkpoint，不回滚或补偿已经开始或完成的业务副作用。
- 完整 CurrentItinerary 由后端独立返回，不重复写入 Conversation。

## 5. 范围控制

- 不因“以后可能用到”而提前创建模块、接口、配置或依赖。
- 不擅自增加多 Agent、微服务、消息总线、复杂缓存、通用规则引擎等架构。
- 不把本项目扩展成通用 Agent 平台、旅行社区、内容平台或完整旅游 ERP。
- 不进行与当前任务无关的重构。
- 新需求超出当前阶段时，先说明影响并等待用户确认，不能自行扩大范围。
- 优先完成一条可运行、可测试的垂直链路，再进行下一阶段。

## 6. 代码设计原则

- 优先使用简单函数、Pydantic 模型、TypedDict State 和必要的 Protocol。
- 一个文件和一个类型应有清晰、单一的职责。
- 只有出现真实复用需求时才提取公共抽象。
- 不为只有一个实现的对象提前设计复杂工厂、注册中心或多层封装。
- 不复制供应商原始响应到整个系统，应在边界处转换成稳定模型。
- 数据模型只表达当前业务实际需要的字段。
- Python 标识符、类型名、函数名和枚举值保持英文，并遵循 Python 社区惯例。

## 7. 中文注释与说明

- 新增或修改的模块说明、Docstring、代码注释和面向用户的异常信息使用中文。
- 关键类、非直观业务规则、状态边界和设计原因应附带简洁中文说明。
- 不要求给每一行代码写注释；能从命名直接看懂的代码不重复解释。
- 注释重点说明“为什么”和业务边界，不机械翻译代码行为。
- LangGraph、ReAct、State、Tool、API 等通用技术名词可以保留英文。

## 8. 避免防御性代码堆砌

- 只在外部输入、模型输出、供应商响应和关键业务不变量处进行必要校验。
- 不为类型系统已经保证的情况重复检查。
- 不添加无法触发或没有明确处理策略的异常分支。
- 不使用宽泛的 `except Exception` 隐藏真实错误。
- 不在 MVP 阶段提前加入复杂重试、熔断、补偿和降级框架；真实外部调用需要时再按场景增加。
- 不通过大量包装类、辅助类或重复 DTO 制造表面上的“完备性”。
- 校验失败信息应直接、明确，并说明用户或调用方能够采取的行动。

## 9. LangGraph 约定

- 编写 LangGraph 代码前阅读项目级 `langgraph-fundamentals` skill。
- 涉及 checkpoint、thread、Store 或子图持久化时阅读 `langgraph-persistence` skill。
- 根图和子图使用各自最小 State，通过明确映射交换数据。
- 消息字段使用 LangGraph 提供的消息 reducer；其他字段默认覆盖，除非确有合并需求。
- `thread_id`通过运行配置传递，不写入 Graph State。
- ReAct 循环必须限制最大步数、工具范围和超时。
- Skill 只指导如何实现当前任务，不能用于扩大项目范围。

## 10. 测试与完成标准

- 使用项目虚拟环境执行命令：`.venv\Scripts\python.exe`。
- 行为变更应包含对应测试；纯注释或文档修改至少运行现有测试和静态检查。
- 基本验证命令：

```powershell
.\.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider
.\.venv\Scripts\python.exe -m ruff check src tests
```

- 未运行验证命令前，不得声称任务已经完成或测试已经通过。
- 交付时说明修改范围、验证结果和仍未实现的边界。
